'''NAME
Ejercicio_gb
VERSION
1.0
AUTHOR
Andres Rivera Ramirez
DESCRIPTION
Programa que accede a un archivo genbank y obtiene la fecha, el organismo y donde se aislo dicho organismo.
CATEGORY.
USAGE
    
ARGUMENTS
  None
SEE ALSO
'''
from Bio import SeqIO

# Se guarda la ruta absoluta del archivo en un string
archivo = r"C:\Users\andre\Downloads\Archivos_clase2 (1)\virus.gb"

# Por cada registro en el archivo se imprime la fecha, el organismo y donde se aislo, accediendo a traves del
# atributo annoations y features
for record in SeqIO.parse(archivo, 'genbank'):
    print(record.annotations['date'])
    print(record.annotations['organism'])
    print(record.features[0].qualifiers['isolation_source'])
